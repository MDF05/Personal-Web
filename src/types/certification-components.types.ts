import { CertificationTypes } from "./certifications.types";

export interface CertificationComponentsProps {
  isVisibleElements?: boolean;
  showHeader?: boolean;
  certificationsSort?: CertificationTypes[];
}
